# Runtime dependencies for the Module-O Python sidecar.
websockets>=12.0,<14
httpx>=0.27,<1.0

# Stdlib `tomllib` is 3.11+; on 3.10 use the backport.
tomli>=2.0; python_version < "3.11"

# Test-only — safe to install alongside; not imported at runtime.
pytest>=8.0,<9
pytest-asyncio>=0.23,<1.0
